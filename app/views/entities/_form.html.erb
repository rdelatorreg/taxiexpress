<%= simple_form_for @entity do |f| %>
  <%= f.input :Description %>
  <%= error_span(@entity[:Description]) %>

  <%= f.button :submit %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                entities_path, :class => 'btn btn-danger' %>
<% end %>
